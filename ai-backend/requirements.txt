# uv pip install -r requirements.txt
fastapi
uvicorn

# LangChain의 기본 메시지-프롬프트 기능
langchain-core
# LangChain에서 ollama 모델을 호출하는 기능
langchain-ollama

# Python에서 다른 서버에 HTTP 요청을 보내는 패키지 (fetch 또는 axios와 비슷)
httpx